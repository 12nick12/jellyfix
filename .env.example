# ==========================================
# 🎟️ JELLYFIX - CONFIGURATION
# ==========================================
# Instructions:
# 1. Copy this file and rename it to ".env"
# 2. Fill in the values below without spaces around "="
# 3. Restart the Docker container after each change
# ==========================================

# --- 🌍 GENERAL ---

# Language for Admin Interface and Emails (Options: EN, FR)
LANGUAGE=EN

# Base application path (Must match your Reverse Proxy configuration)
# Default: /jellyfix
ROOT_PATH=/jellyfix

# Timezone for logs and database (e.g., Europe/London or UTC)
TZ=UTC


# --- 🔒 SECURITY ---

# Jellyfin Admin User ID.
# Used to secure access to the dashboard (https://your-site/jellyfix/admin).
# Where to find it? In Jellyfin > Dashboard > Users > Click your profile > The ID is in the browser URL (e.g., userId=xxxxxxxx...)
JELLYFIN_ADMIN_ID=


# --- 📧 SMTP CONFIGURATION (EMAILS) ---

# Your email provider's SMTP server
# Examples: 
# - Gmail: smtp.gmail.com
# - Ionos: smtp.ionos.fr
# - Outlook: smtp.office365.com
SMTP_SERVER=smtp.example.com

# SMTP Port
# - 587: Recommended (STARTTLS) - Security is handled automatically.
# - 465: Implicit SSL - Also supported.
SMTP_PORT=587

# Full email address used for sending
SMTP_USER=your_email@example.com

# Email password (or App Password if 2FA is enabled)
# If your password contains special characters (!, #, $), wrap it in single quotes '...'
SMTP_PASSWORD='your_password_here'

# Address that will appear as sender (often same as SMTP_USER)
EMAIL_FROM=your_email@example.com

# Address receiving ticket alerts (The Admin)
EMAIL_TO=admin@example.com
